apiVersion: v1
kind: Service
metadata:
  name: servicea
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: servicea
---
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    velocity.tech.v1/id: servicea # Velocity service identifier
    velocity.tech.v1/dependsOn: serviceb
  name: servicea
  labels:
    app: servicea
spec:
  selector:
    matchLabels:
      app: servicea
  replicas: 1
  template:
    metadata:
      labels:
        app: servicea
    spec:
      containers:
        - name: servicea
          image: yeasy/simple-web:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: SERVICE_B_URL
              value: "http://{velocity.v1:serviceb.exposures(port=http).host}:{velocity.v1:serviceb.exposures(port=http).port}"
---
apiVersion: v1
kind: Service
metadata:
  name: serviceb
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: serviceb
---
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    velocity.tech.v1/id: serviceb # Velocity service identifier
    velocity.tech.v1/exposures-http-scheme: http

  name: serviceb
  labels:
    app: serviceb
spec:
  selector:
    matchLabels:
      app: serviceb
  replicas: 1
  template:
    metadata:
      labels:
        app: serviceb
    spec:
      containers:
        - name: serviceb
          image: yeasy/simple-web:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
---
